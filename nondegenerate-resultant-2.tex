\documentclass{standalone}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.12,width=5cm}%
\input{ColorScheme}
\begin{document}
{
\newcommand{\lmbda}{1}
\newcommand{\thta}{atan( (1/2)*(\lmbda+sqrt(\lmbda^2+4)) )}
\newcommand{\major}{1/(sqrt(1+(\lmbda/2)*(\lmbda+sqrt(\lmbda^2+4))))}
\newcommand{\minor}{1/(sqrt(1+(\lmbda/2)*(\lmbda-sqrt(\lmbda^2+4))))}
\begin{tikzpicture}
\newcommand{\mx}{2}
\colorlet{f}{curveOne}
\colorlet{g}{curveTwo}
\colorlet{h}{curveThree}
\draw[very thick,h] ({(1-(sqrt(3)*\lmbda))/2},-\mx) -- ({(1-(sqrt(3)*\lmbda))/2},\mx);
\draw[very thick,h] ({(1+(sqrt(3)*\lmbda))/2},-\mx) -- ({(1+(sqrt(3)*\lmbda))/2},\mx);
\begin{scope}[rotate=\thta]
\draw[very thick,f] (0,0) ellipse ({\major} and {\minor});
\node[very thick,above left,f] at (-.7,1.2) {\(B=0\)};
\end{scope}
\begin{scope}
\begin{scope}[rotate=\thta]
\draw[very thick,g] ({cos(-\thta)},{sin(-\thta)}) ellipse ({\major} and {\minor});
\node[below right,g] at (1.5,-1) {\(C=0\)};
\end{scope}
\end{scope}
\end{tikzpicture}
}
\end{document}
